select upper('variablz')
select lower ('VARIABLZ')
select length ('variablz')
select left('variablz',3)
select right('variablz',4)
select substring('variablz',3,3)
select length(ltrim('    variablz'))
select length(rtrim('   variablz   '))
select length(trim('    variablz   '))